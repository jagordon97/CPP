//Program to convert weeks to days
#include <iostream>
using namespace std;

int main() {
    int weeks = 20, days = weeks * 7;
    cout << "Number of days: " << days << " in " << weeks << " weeks" << endl;
    system("pause");
}