{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Debug Terminal",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
        }

    ]
}